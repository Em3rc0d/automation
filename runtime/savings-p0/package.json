{
  "name": "automation-savings-p0-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test test/*.test.js",
    "demo:payment-reminder": "node demo/payment-reminder/run.js",
    "validate": "npm test && node demo/payment-reminder/run.js --assert"
  }
}
